FROM python:3.11-slim

WORKDIR /app

RUN pip install --no-cache-dir requests

COPY ebpf-log-forwarder.py /app/ebpf-log-forwarder.py

ENV PYTHONUNBUFFERED=1

CMD ["python", "/app/ebpf-log-forwarder.py"]
